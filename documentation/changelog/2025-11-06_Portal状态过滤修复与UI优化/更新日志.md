# Portal状态过滤修复与UI优化

> **变更类型**：Bug修复 + UI优化  
> **影响范围**：Portal模块（前后端）  
> **提交记录**：commit `5b4868e`  
> **更新日期**：2025年11月6日

---

## 问题描述

### 现象

用户访问Portal页面（https://lugarden.space）时，页面显示：

```
暂无可用宇宙
目前还没有已上线的宇宙项目，请稍后再来探索吧～
```

但实际数据库中存在 **2个宇宙记录**，其中"周与春秋练习"的状态为 `published`（已发布），理应显示。

### 服务器日志证据

```
[2025-11-06T06:02:55.474Z] [Portal API] 从数据库查询到 2 个宇宙，已按业务优先级排序
```

后端成功查询到2个宇宙，但前端未能正确展示。

---

## 问题诊断

### 第一步：检查前端过滤逻辑

检查 `lugarden_universal/frontend_vue/src/modules/portal/stores/portal.ts`：

```typescript:149:155:lugarden_universal/frontend_vue/src/modules/portal/stores/portal.ts
  // 计算可见的宇宙（只显示published状态）
  visibleUniverses: (state): Universe[] => {
    return state.universes.filter(
      (universe) => universe.status === 'published'
    )
  },
```

前端预期接收 `status = 'published'` 的宇宙。

### 第二步：检查API返回数据

检查 `lugarden_universal/application/src/routes/portal.js`：

```javascript:27:36:lugarden_universal/application/src/routes/portal.js
function mapStatusToFrontend(dbStatus) {
  const statusMapping = {
    'published': 'active',        // 已发布 → 活跃
    'draft': 'developing',        // 草稿 → 开发中
    'maintenance': 'maintenance', // 维护 → 维护中
    'archived': 'archived'        // 归档 → 已归档
  };
  
  return statusMapping[dbStatus] || 'developing';
}
```

**问题根源找到**：API层将 `published` 映射为 `active`！

### 数据流分析

```
数据库 → API层 → 前端
published → active → 找不到！(期望published)
```

1. 数据库：`status = 'published'`
2. API映射：`'published'` → `'active'`
3. 前端接收：`status = 'active'`
4. 前端过滤：`universe.status === 'published'` → **匹配失败**
5. 结果：0个宇宙显示

---

## 解决方案决策

### 方案对比

| 方案 | 描述 | 优点 | 缺点 | 决策 |
|------|------|------|------|------|
| **方案A** | 修改前端过滤逻辑，适配API映射 | 改动小（仅前端） | 前后端语义不一致，增加认知负担 | ❌ 拒绝 |
| **方案B** | 删除API层状态映射，前后端统一 | ✅ 语义统一<br>✅ 符合RESTful原则<br>✅ 降低维护成本 | 需要修改前后端 | ✅ **采纳** |

### 决策依据

1. **数据一致性原则**：前后端应使用相同的状态枚举值
2. **最小惊讶原则**：数据库存储的值应与API返回的值一致
3. **长期维护性**：减少不必要的映射层，降低bug风险

---

## 实施细节

### 1. 后端修复（3处修改）

#### 修改1：删除状态映射逻辑

**文件**：`lugarden_universal/application/src/routes/portal.js`

```diff
function mapStatusToFrontend(dbStatus) {
-  const statusMapping = {
-    'published': 'active',
-    'draft': 'developing',
-    'maintenance': 'maintenance',
-    'archived': 'archived'
-  };
-  
-  return statusMapping[dbStatus] || 'developing';
+  // 前后端统一使用: published, draft, maintenance
+  return dbStatus || 'draft';
}
```

**理由**：消除状态映射，前后端直接使用数据库原始值。

#### 修改2：修正业务优先级排序

**文件**：`lugarden_universal/application/src/routes/portal.js`

```diff
const statusPriority = {
  'published': 1,   // 已发布 - 最高优先级
-  'active': 1,      // 活跃状态 - 最高优先级
-  'developing': 2,  // 开发中 - 第二优先级
  'draft': 2,       // 草稿 - 第二优先级  
  'maintenance': 3, // 维护中 - 第三优先级
-  'archived': 4     // 已归档 - 最低优先级
};
```

**理由**：移除已废弃的 `active` 和 `developing` 状态，与实际数据库模式对齐。

#### 修改3：修正可访问性判断

**文件**：`lugarden_universal/application/src/routes/portal.js`

```diff
accessibility: {
-  isAccessible: universe.status === 'published' || universe.status === 'active',
+  isAccessible: universe.status === 'published',
-  accessMessage: universe.status !== 'published' && universe.status !== 'active' 
+  accessMessage: universe.status !== 'published' 
    ? '此宇宙正在开发中，暂时无法访问' 
    : undefined,
  requiresAuth: false
},
```

**理由**：仅 `published` 状态的宇宙可访问，移除已废弃的 `active` 判断。

### 2. 前端优化（2处修改）

#### 修改1：PC端卡片宽度优化

**问题**：单个宇宙卡片占满1200px宽度，在PC端显得过宽。

**解决方案**：

**文件**：`lugarden_universal/frontend_vue/src/modules/portal/views/UniversePortal.vue`

```diff
.universes-grid {
  display: grid;
  gap: 2rem;
  justify-content: center;
-  grid-template-columns: repeat(auto-fit, minmax(400px, 600px));
+  grid-template-columns: repeat(auto-fit, minmax(400px, 500px));
-  max-width: min(100%, 1240px);
+  max-width: min(100%, 1040px);
  margin: 0 auto;
}
```

**效果**：
- 单卡片最大宽度：600px → 500px
- 两卡片容器宽度：1240px → 1040px（500px × 2 + 2rem间距）
- 视觉更紧凑，更符合阅读习惯

#### 修改2：状态枚举同步

**文件**：`lugarden_universal/frontend_vue/src/modules/portal/types/portal.ts`

```diff
export type UniverseStatus = 
  | 'published'     // 已发布（对外展示）
-  | 'active'        // 活跃状态（已废弃）
  | 'draft'         // 草稿（开发中）
-  | 'developing'    // 开发中（已废弃）
  | 'maintenance'   // 维护中
-  | 'archived'      // 已归档
```

**理由**：TypeScript类型定义与后端枚举值保持一致。

---

## 验证结果

### 后端验证

**健康检查通过**：
```bash
$ curl http://localhost:3000/api/health
{"ok":true,"db":"ready",...}
```

**API返回正确数据**：
```bash
$ curl http://localhost:3000/api/portal/universes
{
  "universes": [
    {
      "id": "zhou",
      "status": "published",  // ✅ 不再是'active'
      ...
    }
  ],
  "total": 1
}
```

### 前端验证

**浏览器控制台日志**：
```
[Portal] 宇宙列表加载成功: Object
[Portal] 预加载完成, finalLoading: false universes: 2
```

**页面展示**：
- ✅ "周与春秋练习"正常显示（`status = 'published'`）
- ✅ "毛小豆故事演绎"不显示（`status = 'draft'`）
- ✅ PC端卡片宽度500px，视觉效果良好
- ✅ 移动端响应式布局正常

---

## 技术影响评估

### 变更影响范围

| 组件 | 变更类型 | 影响 | 风险等级 |
|------|---------|------|---------|
| `portal.js` API | 逻辑修改 | 状态返回值变化 | 🟡 中（需要rebuild） |
| `portal.ts` Store | 类型定义 | 移除废弃状态 | 🟢 低 |
| `UniversePortal.vue` | 样式优化 | Grid布局调整 | 🟢 低 |

### 兼容性分析

**向后兼容性**：✅ 兼容
- 数据库无需迁移（状态值未变）
- 前端组件无破坏性变更
- API响应结构不变（仅status字段值变化）

**部署要求**：
- ✅ 前端需重新构建：`npm run build`
- ✅ 后端需重新构建镜像：`docker-compose build --no-cache app`
- ❌ 无需数据库迁移

---

## 附加UI优化细节

### 卡片高度调整过程

**初始状态**：`min-height: 200px`

**尝试1**：增加到 `280px`
- 效果：PC端更舒展，但移动端偏高
- 决策：回退

**尝试2**：移动端单独设置 `220px`
- 效果：增加复杂度，响应式维护成本上升
- 决策：回退

**最终方案**：保持 `200px`，PC和移动端统一
- 理由：简洁性优先，当前高度已足够

### 宽度调整迭代

**初始方案**：`minmax(400px, 600px)`
- 问题：单卡片在1200px容器内显得过宽

**最终方案**：`minmax(400px, 500px)`
- 优点：
  - 单卡片更紧凑（500px）
  - 两卡片并排布局合理（1040px总宽）
  - 保持响应式（最小400px保证移动端可用）

---

## 运维注意事项

### VPS部署流程

**本次修改需要**：
1. ✅ 后端代码rebuild（状态映射逻辑变更）
2. ✅ 前端重新构建（Vue组件和stores变更）
3. ❌ 数据库无需变更

**部署命令**（VPS上执行）：

```bash
# 1. 拉取代码
cd /root/lu_garden_lab
git pull

# 2. 前端构建（VPS本地，非Docker）
cd lugarden_universal/frontend_vue
npm run build  # 预计<14秒
cd ../..

# 3. 后端重建（Docker）
docker-compose down
docker-compose build --no-cache app
docker-compose up -d

# 4. 验证
sleep 30  # 等待健康检查
docker-compose ps
curl http://localhost:3000/api/health
```

### 验证清单

- [ ] 容器状态：`docker-compose ps` → 所有容器 `Up (healthy)`
- [ ] API健康：`curl localhost:3000/api/health` → `{"ok":true}`
- [ ] 宇宙显示：访问 https://lugarden.space → 显示"周与春秋练习"
- [ ] 草稿隐藏：确认"毛小豆故事演绎"不显示
- [ ] PC端布局：卡片宽度约500px，居中显示
- [ ] 移动端布局：单列布局，卡片占满宽度

---

## 经验总结

### 问题根源分析

**核心问题**：不必要的中间映射层

```
数据库  →  API映射层  →  前端
published → active    → 期望published (❌ 不匹配)
```

**正确架构**：

```
数据库  →  API  →  前端
published → published → ✅ 匹配
```

### 设计原则确认

1. **数据一致性**：前后端使用相同的枚举值
2. **语义准确性**：状态名称应反映实际含义（`published` 优于 `active`）
3. **减少映射层**：避免不必要的数据转换
4. **类型安全**：TypeScript类型定义与后端对齐

### 适用场景

**何时需要状态映射**：
- ✅ 与第三方API集成（无法控制其枚举值）
- ✅ 数据库遗留系统迁移（渐进式重构）
- ✅ 多语言支持（i18n显示文本）

**何时不需要状态映射**：
- ❌ 前后端自主设计的系统
- ❌ 枚举值已经语义清晰
- ❌ 无兼容性历史包袱

---

## 相关文档

- [Portal API合同规范](../../backend/api-contracts-v2025.08.28.md)
- [Vue TypeScript标准](.cursor/rules/vue-typescript-standards.mdc)
- [Docker部署指南](../../deployment/陆家花园Docker部署指南.md)

---

## 提交信息

```
commit 5b4868e43ca6187f52b7e37413bab19b5fc03a7c
Author: XiEr <402323607@qq.com>
Date:   Thu Nov 6 14:26:26 2025 +0800

    fix(portal): 修复状态过滤逻辑并优化PC端布局

    - 删除API层状态映射，前后端统一使用published/draft/maintenance
    - 修复业务优先级排序，移除已废弃的active/developing状态
    - 修正可访问性判断，只检查published状态
    - 优化PC端卡片宽度为500px，提升单卡片显示效果
    - 统一PC和移动端卡片高度为200px

    解决了Portal页面无法正确显示已发布宇宙的问题

 .../application/src/routes/portal.js               | 26 +++++-------
 .../src/modules/portal/components/UniverseCard.vue | 12 +++---
 .../src/modules/portal/stores/portal.ts            | 47 +++++++++-------------
 .../src/modules/portal/types/portal.ts             |  2 +-
 .../src/modules/portal/views/UniversePortal.vue    | 12 ++++--
 5 files changed, 43 insertions(+), 56 deletions(-)
```

---

**变更完成日期**：2025年11月6日  
**下次部署时间**：待VPS运维执行  
**预期影响**：Portal页面恢复正常，用户可见已发布宇宙

